# 更新日志 (Changelog)

## [0.1.1] - 2025-11-11

### 🎉 新增功能

#### 💎 用户设置系统
- **头像上传功能**
  - 支持格式：JPG, PNG, GIF, WEBP
  - 文件大小限制：最大 5MB
  - 实时预览和裁剪显示
  - 自动删除旧头像文件
  - 头像存储：`uploads/avatars/` 目录

- **用户名修改**
  - 用户名长度验证（2-50个字符）
  - 重复用户名检测
  - 实时更新所有显示位置

- **点击头像进入设置**
  - 移除独立设置按钮，界面更简洁
  - 点击侧边栏头像打开设置模态窗口
  - 头像悬停效果（放大 + 蓝色阴影）
  - 鼠标提示："点击设置个人信息"
  - 仅登录状态下可点击

#### 🎨 管理后台头像显示
- **用户管理表格**
  - 新增头像列（40px 圆形显示）
  - 表格列数从5列扩展到6列

- **对话管理**
  - 用户列表显示 36px 头像
  - 详情页显示完整用户头像和信息

- **积分管理**
  - 用户列表显示 36px 头像
  - 积分详情页显示用户头像
  - 头像与积分信息并列展示

- **头像显示策略**
  - 三级回退机制：
    1. 实际上传的头像图片
    2. 用户名首字母（渐变蓝色圆形）
    3. 问号占位符（灰色圆形）

#### 📦 Chart.js 本地化
- **解决浏览器 tracking prevention 警告**
  - 浏览器隐私保护功能会阻止 CDN 访问
  - 通过 npm 安装 chart.js@4.4.1
  - 复制到 public 目录作为静态资源
  - 更新 admin.html 使用本地引用

- **优势**
  - 提高页面加载稳定性（不依赖外部网络）
  - 增强用户隐私保护
  - 支持离线环境使用
  - 减少外部请求

### ✨ 优化改进

#### 用户界面
- 简化侧边栏操作区域（移除设置按钮）
- 统一头像显示样式和尺寸
- 优化头像悬停交互动画
- 提升界面一致性和视觉美感

#### 用户体验
- 更直观的设置入口（点击头像）
- 平滑的过渡动画效果
- 清晰的视觉反馈

### 🐛 Bug修复

#### 代码块复制功能
- **问题**：历史对话中的代码块复制按钮不工作
- **原因**：使用 `outerHTML` 重新赋值导致事件监听器丢失
- **修复**：使用 `appendChild` 方法保留事件绑定
- **影响范围**：所有包含代码块的历史对话

#### 头像 404 错误
- **问题**：上传头像后访问返回 404
- **原因**：目录结构不正确，缺少 `avatars` 子目录
- **修复**：确保 `uploads/avatars` 目录存在
- **位置**：server.js avatarUpload 中间件

### 🔧 技术细节

#### 新增 API
- `POST /api/auth/avatar` - 上传用户头像
  - 支持格式：jpeg, jpg, png, gif, webp
  - 大小限制：5MB
  - 返回头像 URL

- `PUT /api/auth/profile` - 更新用户信息
  - 支持修改用户名
  - 用户名唯一性检测

#### 数据库变更
- `users` 表：`avatar` 字段（已存在则跳过添加）

#### 文件变更
- **新增**
  - `public/user-settings.js` - 用户设置功能模块
  - `public/chart.umd.min.js` - Chart.js 本地文件（201KB）
  - `uploads/avatars/` - 头像存储目录

- **修改**
  - `public/app.js` - 集成用户设置，移除 settingsBtn 引用
  - `public/index.html` - 用户设置模态窗口，移除设置按钮
  - `public/styles.css` - 头像悬停效果和设置样式
  - `public/admin.js` - 头像显示函数 `getUserAvatarHTML()`
  - `public/admin.html` - 表格头像列，Chart.js 本地引用
  - `server.js` - 头像上传和用户信息更新 API
  - `package.json` - 添加 chart.js@4.4.1 依赖

### 📊 功能亮点

#### 头像显示位置
1. 侧边栏用户信息区域（可点击进入设置）
2. 对话消息中的用户头像
3. 管理后台用户管理表格
4. 管理后台对话管理列表和详情
5. 管理后台积分管理列表和详情

#### 设置模态窗口功能
1. 头像预览和上传
2. 用户名修改
3. 实时保存和更新
4. 优雅的错误提示

---

## [2.1.0] - 2025-11-03

### 🎉 新增功能

#### 💎 完整的积分系统
- **积分显示组件**
  - 侧边栏积分条：显示当前积分余额和管理入口
  - 输入框积分徽章：实时显示剩余积分，点击可打开积分管理界面
  - 积分不足自动警告（<3积分时变红色并抖动）

- **积分管理中心**
  - 积分概览：显示当前积分、累计获得、累计消耗
  - 每日签到：获得1-3积分，连续签到奖励递增
  - 邀请好友：生成邀请码，双方各得5积分
  - 积分历史：显示最近交易记录

- **邀请功能增强**
  - 自动生成完整的邀请URL（而非仅邀请码）
  - 一键复制分享文案（包含邀请词和链接）
  - 系统原生分享支持（移动端）
  - 优雅的复制成功提示动画

#### 🎨 UI/UX优化
- **积分徽章交互**
  - 鼠标悬停放大动画（scale 1.1）
  - 点击反馈缩小效果（scale 0.95）
  - 点击打开积分管理界面
  - 弹跳入场动画

- **视觉设计**
  - 蓝白科技风格一致性
  - 流畅的过渡动画（0.3s ease）
  - 积分不足时的警告样式
  - 响应式设计适配移动端

#### 📱 管理后台
- 用户管理：查看、搜索、删除用户
- 对话管理：查看用户对话内容
- 积分管理：手动调整用户积分
- 数据概览：系统统计信息

### 🐛 Bug修复

#### 关键修复：积分API 500错误
- **问题**：所有积分API返回500内部服务器错误
- **原因**：API使用了错误的 `req.user.userId`（应该是 `req.userId`）
- **影响**：导致积分数据无法加载，用户看不到积分显示
- **修复位置**：
  - `GET /api/points` - 获取积分信息
  - `POST /api/points/checkin` - 每日签到
  - `POST /api/points/invite` - 生成邀请码
  - `GET /api/points/transactions` - 积分历史

#### 前端优化
- 修复刷新页面后积分不显示的问题
- 在 `updateLoginState()` 中添加积分加载逻辑
- 退出登录时正确隐藏所有积分显示（侧边栏+输入框徽章）

### 🔧 技术改进

#### 后端 (server.js)
- 统一使用 `req.userId` 获取用户ID
- 添加详细的错误日志（console.error）
- 改进积分API的错误处理

#### 前端
- **app.js**
  - `updateLoginState()` 现在会自动加载积分
  - 优化登录/退出流程

- **points.js** (新增)
  - 模块化积分系统实现
  - 支持签到、邀请、积分查询
  - 增强的分享功能

- **points-styles.css** (新增)
  - 独立的积分样式文件
  - 响应式设计
  - 动画效果定义

#### 文件结构
```
public/
├── admin.html          # 管理后台页面（新增）
├── admin.js           # 管理后台逻辑（新增）
├── points.js          # 积分系统逻辑（新增）
├── points-styles.css  # 积分样式（新增）
├── app.js             # 主应用（优化）
├── index.html         # 主页面（优化）
└── styles.css         # 主样式（优化）
```

### 📚 文档更新

#### 新增文档
- **POINTS_SYSTEM_GUIDE.md** - 积分系统完整功能指南
- **POINTS_DEBUG.md** - 调试和测试步骤
- **BUGFIX-POINTS-500.md** - 500错误修复报告
- **README-FOR-AI.md** - AI开发文档
- **CHANGELOG.md** - 本更新日志

#### 文档内容
- 完整的功能说明
- 详细的使用指南
- 调试和排错步骤
- API接口文档

### 🎯 功能对比

#### 修复前
- ❌ 积分API返回500错误
- ❌ 刷新页面后积分消失
- ❌ 积分徽章不可点击
- ❌ 邀请功能仅显示邀请码

#### 修复后
- ✅ 积分API正常返回（200 OK）
- ✅ 刷新页面积分保持显示
- ✅ 积分徽章可点击打开管理界面
- ✅ 邀请功能生成完整URL和分享文案

### 📊 数据流程

```
用户登录
  ↓
checkAuth() → 验证Token
  ↓
updateLoginState(true)
  ↓
PointsSystem.load()
  ↓
GET /api/points → 200 OK
  ↓
显示积分：侧边栏 + 输入框徽章
```

### 🔒 安全性

#### .gitignore 优化
- 忽略环境变量文件 (.env)
- 忽略数据库文件 (*.sqlite)
- 忽略上传文件 (uploads/)
- 忽略开发工具文件 (.spec-workflow/)
- 忽略IDE配置 (.vscode/, .idea/)

### 🚀 部署说明

#### 环境变量配置
确保 `.env` 文件包含以下必需配置：
```env
DIFY_API_KEY=your-dify-api-key
DIFY_BASE_URL=your-dify-url
JWT_SECRET=your-jwt-secret
SESSION_SECRET=your-session-secret
ADMIN_INIT_KEY=your-admin-key
```

#### 数据库初始化
首次运行时会自动创建SQLite数据库和所需表结构。

#### 启动服务
```bash
npm install
npm start
```

### 🎨 积分规则

| 操作 | 积分变化 | 说明 |
|------|---------|------|
| 新用户注册 | +10 | 初始积分 |
| 每日签到 | +1~3 | 连续签到奖励递增 |
| 邀请新用户 | +5 | 双方各得5积分 |
| AI对话 | -1 | 每次对话消耗1积分 |

### 🔄 升级步骤

对于现有项目升级：

1. **拉取最新代码**
```bash
git pull origin main
```

2. **安装依赖**（如有新增）
```bash
npm install
```

3. **检查环境变量**
```bash
cp .env.example .env
# 编辑 .env 填写配置
```

4. **重启服务**
```bash
npm start
```

5. **测试功能**
- 登录账号
- 检查积分显示
- 测试签到功能
- 测试邀请功能

### 🐛 已知问题

无已知严重问题。

### 📅 下一步计划

#### 可能的功能扩展
- [ ] 积分商城（用积分兑换特权）
- [ ] 签到日历（可视化连续签到）
- [ ] 积分排行榜
- [ ] 成就系统
- [ ] 分享海报生成
- [ ] 积分任务系统

### 🙏 致谢

感谢所有使用和支持本项目的用户！

---

## [2.0.0] - 2025-01-XX (基础版本)

### 初始功能
- AI对话系统
- 用户认证（注册/登录）
- 对话管理
- 文件上传
- Markdown渲染
- 流式响应

---

**版本格式说明**: [主版本号.次版本号.修订号]
- 主版本号：重大架构变更
- 次版本号：新功能添加
- 修订号：Bug修复和小优化
